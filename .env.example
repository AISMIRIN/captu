# Copy to .env and fill in your values.
# docker compose reads .env automatically.

# Host path to the recordings directory (mounted read-only inside the container).
CAPTU_NAS_HOST=/mnt/your/recordings
